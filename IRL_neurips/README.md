# IRL NeurIPS Compatibility Namespace

The active GenPQR conference-paper code now lives in
`experiments/irl/conference_genpqr/repro`.

This directory is a thin compatibility layer for old imports such as
`import IRL_neurips.data_generation` and old paths that expected
`IRL_neurips/IRL_neurips_paper_repro`.
