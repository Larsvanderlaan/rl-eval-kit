# Diagnostics

`GenPQRResult.diagnostics` remains a flat dictionary.
`GenPQRResult.diagnostics_report` exposes the typed `GenPQRDiagnostics`
dataclass with policy, Q, reward, normalization, anchor, and warning fields.
